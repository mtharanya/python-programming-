def word count(str):
       count=dict()
       words=str.split()
 for wort in wodts:
 if word in counts:
       counts[word]+=1
 else:
       count[word]=1
    return count
  print(word count('the quick brown for jumps over the lazy cat.'))
